import { Box, Typography } from '@mui/material';

export default function Randomization() {
  return (
    <Box>
      <Typography variant="h4">Randomization</Typography>
      <Typography sx={{ mt: 2 }}>Randomization interface will appear here...</Typography>
    </Box>
  );
}
