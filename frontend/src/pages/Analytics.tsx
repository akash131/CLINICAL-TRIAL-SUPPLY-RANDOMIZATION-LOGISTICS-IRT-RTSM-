import { Box, Typography } from '@mui/material';

export default function Analytics() {
  return (
    <Box>
      <Typography variant="h4">Analytics</Typography>
      <Typography sx={{ mt: 2 }}>Analytics dashboard will appear here...</Typography>
    </Box>
  );
}
