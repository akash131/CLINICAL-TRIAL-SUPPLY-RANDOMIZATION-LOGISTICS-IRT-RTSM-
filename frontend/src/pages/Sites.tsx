import { Box, Typography } from '@mui/material';

export default function Sites() {
  return (
    <Box>
      <Typography variant="h4">Sites</Typography>
      <Typography sx={{ mt: 2 }}>Sites interface will appear here...</Typography>
    </Box>
  );
}
