import { Box, Typography } from '@mui/material';

export default function Inventory() {
  return (
    <Box>
      <Typography variant="h4">Inventory</Typography>
      <Typography sx={{ mt: 2 }}>Inventory interface will appear here...</Typography>
    </Box>
  );
}
